apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: talentflow
  labels:
    app: api-gateway
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: api-gateway
