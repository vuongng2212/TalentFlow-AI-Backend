apiVersion: v1
kind: Service
metadata:
  name: cv-parser-service
  namespace: talentflow
  labels:
    app: cv-parser
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: cv-parser
