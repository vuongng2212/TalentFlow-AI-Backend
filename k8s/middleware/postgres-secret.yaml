apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: talentflow
type: Opaque
stringData:
  # Replace with a strong generated password before deploying to production.
  password: "your-strong-postgres-password"
