http.host: "0.0.0.0"
xpack.monitoring.enabled: false

# Pipeline workers — tune based on available CPU cores
pipeline.workers: 2
pipeline.batch.size: 125
pipeline.batch.delay: 50

# Log level for Logstash itself
log.level: warn
